name: flightdeck_remote
description: Proxies remotos dos contratos do FlightDeck (plano 58) — o cliente fala com o flightdeck-server pelo protocolo.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.9.0

dependencies:
  flightdeck_core:
    path: ../flightdeck_core
  flightdeck_protocol:
    path: ../flightdeck_protocol

dev_dependencies:
  test: ^1.25.0
